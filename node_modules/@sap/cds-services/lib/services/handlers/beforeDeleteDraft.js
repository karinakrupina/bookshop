const { addDraftDataFromExistingDraft } = require('../utils/handlerUtils')
/**
 * Generic Handler for before DELETE requests.
 *
 * @alias module:handlers.beforeDeleteDraft
 */
const beforeDeleteDraft = ({ service }) => async context => {
  await addDraftDataFromExistingDraft(context, service)
  context.isDraftChange = true
  context.draft = context.data.DraftAdministrativeData_DraftUUID // deprecated
}

module.exports = beforeDeleteDraft
